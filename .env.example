# Set automatically by `npx convex dev` into .env.local.
# When deploying to Vercel, copy the production deployment URL from the Convex dashboard.
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
